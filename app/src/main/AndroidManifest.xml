<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.androidev.cursocomposeestados">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CursoComposeEstados">
        <activity
            android:name=".tareas_app.TaskActivity"
            android:exported="true"
            android:label="@string/title_activity_task"
            android:theme="@style/Theme.CursoComposeEstados.NoActionBar" />
        <activity
            android:name=".compose_viewmodel.ItemActivity"
            android:exported="true"
            android:label="@string/title_activity_item"
            android:theme="@style/Theme.CursoComposeEstados.NoActionBar" />
        <activity
            android:name=".flujo_datos_unidireccional.ItemExampleViewModelActivity"
            android:exported="true" />
        <activity
            android:name=".estado_no_estructurado.ItemExampleActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CursoComposeEstados.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>